[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ometa-diff"
version = "0.1.0b1"
description = "MCP server adding metadata version-diff intelligence to OpenMetadata"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.10"
keywords = ["openmetadata", "metadata", "diff", "changelog", "mcp", "data-catalog"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries",
]

dependencies = [
    "httpx>=0.27.0",
    "pydantic>=2.7.0,<3.0",
    "typer>=0.12.0",
    "rich>=13.0.0",
    "python-dateutil>=2.9.0",
]

[project.optional-dependencies]
mcp = ["mcp>=1.0.0"]
dev = ["pytest>=8.0.0", "ruff>=0.5.0", "respx>=0.21.0"]

[project.urls]
Homepage = "https://github.com/SamChawla/ometa-diff"
Repository = "https://github.com/SamChawla/ometa-diff"
Issues = "https://github.com/SamChawla/ometa-diff/issues"

[project.scripts]
ometa-diff = "ometa_diff.cli:app"

[tool.hatch.build.targets.wheel]
packages = ["src/ometa_diff"]

[tool.hatch.build.targets.sdist]
exclude = ["docker-volume/", "docker-compose-om.yml"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "integration: requires a live OpenMetadata instance (set OPENMETADATA_HOST and OPENMETADATA_JWT_TOKEN)",
]
